347. Top K Frequent Elements
class Solution(object):
    def topKFrequent(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: List[int]
        """
        cnt=Counter(nums)
        sort_cnt=sorted(cnt.keys(),key=lambda x:cnt[x],reverse=True)
        return sort_cnt[:k]
        
