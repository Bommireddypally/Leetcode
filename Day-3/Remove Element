#27 Remove Element
class Solution:
    def removeElement(self, nums, val):
        k = 0  # Pointer for the position of the next element not equal to val
        
        for i in range(len(nums)):
            if nums[i] != val:
                nums[k] = nums[i]
                k += 1
        
        return k
